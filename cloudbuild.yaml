steps:
  # build the container
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '--network=cloudbuild','-t', 'gcr.io/nuxt-328111/server', '.']
  # push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/nuxt-328111/server']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['run', 'deploy', 'server', '--image', 'gcr.io/nuxt-328111/server', '--region', 'europe-central2', '--platform', 'managed', '--allow-unauthenticated']
images:
  - 'gcr.io/nuxt-328111/server'
